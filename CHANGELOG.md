# Changelog

All notable changes to Graflume will be recorded here.

## Unreleased

- Add the registry-driven `builtInThemeCatalog` and a built-in `r-base` theme pinned to R 4.6.1 base graphics, normalized to a common white 96 dpi PNG/browser canvas: white plot, no grid, black four-sided box and outward ticks, centered bold main title, open-circle points, the R 4 palette, and base-style bar, histogram, box, and pie defaults. Apply it across all 41 Canvas families, all 162 Canvas presets, every Spatial variant, and the integrated globe mode while preserving explicit styles and retaining Graflume geometry where base graphics has no direct counterpart. Generate theme snapshots from the ordered catalog and make CDN examples consume that catalog so future built-ins do not require another fixed theme cycle. The minified default/complete/spatial bundles now measure 384.8/486.7/275.7 KiB, so their bounded budgets increase from 382/484/273 KiB to 385/487/276 KiB.
- Add a built-in `ggplot` theme pinned to ggplot2 4.0.3 `theme_gray()`: separate white plot and grey panel surfaces; theme-aware primary x/y major and minor grids; blank axis baselines; ggplot typography, physical-unit conversions, geom-facing defaults, count-dependent HCL discrete colours, and Lab-interpolated continuous colours. Apply it across all 41 Canvas families, all 162 Canvas presets, every Spatial variant, and the integrated globe mode while preserving explicit spec styles and documenting the exact boundary for chart types with no ggplot2 core counterpart. The minified default/complete/spatial bundles now measure 381.7/483.5/272.7 KiB, so their bounded budgets increase from 374/475/260 KiB to 382/484/273 KiB.
- Add portable annotation visibility controls and lifecycle APIs to Canvas and Spatial charts. The localized top-right button is present only while annotations exist, hides bubbles and connectors without removing highlights or authored accessibility text, resets on `setSpec()`, and emits `annotationvisibilitychange`. Upgrade Canvas and Spatial auto placement to score plot bounds, targets, visible data, controls, inside legends, and earlier callouts while retaining safe authored cardinal placement; add bounded grapheme-aware Canvas wrapping, clipped Scene text, and readable Spatial DOM wrapping/line clamps for unbroken Latin, CJK, emoji, and RTL content.
- Add opt-in stable-key playback transitions without changing discrete frame state or source data. `interaction.playback.key` matches entities across compiled endpoints, including matrix and compound numeric roles without mistaking structural tokens for filtered row indices, while `transition` interpolates compatible geometry, numeric styles, and colors with `linear` or `ease-in-out`; entering and exiting nodes fade and incompatible topology crossfades. Keep transitions off by default, clamp automatic transition duration below the frame interval, settle sparse animation frames before advancing so transient exit nodes cannot accumulate, render only transient Scenes between authoritative endpoints, suppress misleading hit/selection state while shapes move, share one animation-frame owner with playback, and settle or cancel safely for seek, pause, re-render, reduced motion, hidden documents, and destroy. Together with collision-aware Canvas/Spatial callouts, the minified default/complete/spatial bundles now measure 373.1/474.3/257.3 KiB, so their bounded budgets increase from 360/460/252 KiB to 374/475/260 KiB.
- Correct horizontal legend measurement to reserve both outer insets, keeping fitting explicit labels intact while retaining bounded ellipsis for labels that genuinely exceed the available chart width.
- Add a shared, function-free customization layer across all 41 Canvas families and every Spatial/WebGL variant: mark-aware layer/category/continuous legends, keyboard-operated layer/category visibility where semantics permit, static datum/layer/range/plot or 3D point/box highlights, click and programmatic selection, and safe text-only runtime callouts. Expose immutable-base transient state through legend, selection, and annotation APIs plus lifecycle events; preserve configured targets across playback; follow locale/RTL text direction; validate finite JSON scalar targets, unique ids, and existing layer references; and keep large-profile static decorations available while pointer selection follows existing hit-test degradation. Spatial overlays follow projected 3D targets and orbiting cameras, with category/continuous legends descriptive, layer legends toggleable, and true external legend rails distinct from `inside-*` overlays; DOM callouts are intentionally excluded from framebuffer PNG export. The three minified browser entrypoints now measure 357.3/458.5/250.7 KiB for default/complete/spatial, so their bounded raw budgets increase from 325/425/220 KiB to 360/460/252 KiB.
- Add the independent `graflume/spatial` WebGL entrypoint and closed, function-free `SpatialSpec 0.1` contract with 5 spatial mark types, 3 canonical families, 7 variants, and one globe mode integrated into Map. Render actual surfaces, triangle meshes, strictly bounded deterministic volume samples, marching-tetrahedra isosurfaces, vector cones and streamtubes with a 5–48 segment contract, spatial scatter, and a Natural Earth globe; add depth-aware picking, transparent-pass ordering that excludes zero-alpha occluders, text-only tooltips, combined custom descriptions and control instructions, camera orbit/pan/zoom/projection APIs, compact accessible controls, fullscreen, PNG export, bounded geometry generation, WebGL context recovery, generated previews, a runnable browser gallery, JSON Schema, declarations, bundle/CDN verification, and regression coverage. Upgrade the representative gallery to filled, lit, high-density scenes with explicit readable camera framing, scalar-dependent volume alpha, and sphere-shaded point impostors whose discarded opaque edges cannot become invisible depth occluders. Together with the 41 Canvas families and 162 presets, this establishes 44 canonical families and 170 callable presets or modes without duplicating overlapping names.
- Consolidate histogram and five-number summaries into the `distribution` family and radar profiles into `polar`; add actual Canvas Scene compilers for raster images, violin and bivariate distribution modes, polar lines/points/bars, ternary compositions, Smith traces, scatter matrices, and carpet grids with scatter or contour overlays. Align the canonical distribution default, compiler, and axis resolver on histogram mode; give density curves shared derived axes; and apply deterministic whole-mark budgets, bounded violin KDE inputs with exact summary tooltips, plus indexed one-pass grouping to the new row-proportional analytical compilers. Add icicle, funnel-area, parallel-category ribbon, and gauge number/delta/bullet modes while keeping legacy Quick APIs callable, expanding discovery to 41 canonical families and 162 documented presets. The expanded two-dimensional runtime measures 310.5/408.2 KiB for the default/complete minified bundles, so their bounded raw budgets increase from 300/380 KiB to 325/425 KiB; the independent spatial bundle remains at 220 KiB.
- Add opt-in whole-Canvas inspection navigation with wheel, drag, pinch, keyboard, bounded zoom, and reset; compact zoom, fullscreen, PNG export, and playback controls with declarative labels; programmatic view, playback, and fullscreen lifecycle APIs; and discrete `frame`, `cumulative`, or `window` playback over first-occurrence source values. Render the shared controls as a top-right SVG icon strip with distinct hover, focus, pressed, and disabled states; keep coarse-pointer targets at 44 px; and move seek, rate, and loop into an accessible secondary playback panel so the closed bar remains narrow. Keep playback filtering behind an explicit host-approved opt-in, document that inspection is not data-domain or GIS zoom, and publish a 37-family capability matrix with aggregation, interval, financial path-dependence, layout, and accessibility constraints.
- Replace the hand-authored schematic world outlines with a reproducibly generated, built-in Natural Earth Admin-0 1:110m basemap containing 177 country features; preserve portable Canvas 2D output with compound even-odd paths; constrain longitude and latitude to geographic bounds; retain region bubbles while adding choropleth mode and broader country aliases; add optional graticules, attribution, basemap suppression, and map-specific colors and boundary styles; and make every geographic preset, including the historical `tiledMap()` compatibility name, use the same static political basemap without remote tile requests. The embedded boundary snapshot measures 262.4/346.0 KiB minified for the default/complete bundles (83.0/105.4 KiB with gzip-9), with raw minified budgets raised from 120/205 KiB to 270/360 KiB.
- Add a portable four-axis Cartesian system with independently resolved `x`, `x2`, `y`, and `y2` scales; channel-safe `axisId` bindings; declarative number, percent, currency, date, and time formatting; horizontal, vertical, and angled labels; configurable fonts, spacing, strokes, positions, and explicit ticks; reversed scales; secondary-axis tooltip targeting; deep chart/encoding overrides; JSON Schema and runtime validation; and a runnable axis manual while preserving the legacy `title`, `grid`, `tickCount`, `format`, and `labelAngle` forms. The bounded axis compiler, scale, formatting, and renderer additions measure 117.5/202.9 KiB minified for the default/complete bundles, with budgets raised from 97/182 KiB to 120/205 KiB.
- Add the portable `interaction.tooltip.trigger: 'axis'` mode with explicit x/x2/y/y2 selection for ordered charts. Exact mark hits retain priority; other eligible pointer positions resolve the nearest actual datum without interpolation, while existing `tooltip: true` and omitted triggers remain mark-based and structured data events keep exact-hit semantics. Document the pointer-only behavior and accessible table fallback across every generated family guide, and raise the measured minified bundle budgets from 92/176 KiB to 97/182 KiB for the default/complete builds.
- Add opt-in, chart-surface-clamped, text-only hover tooltips with portable chart-specific fields, locale-aware number/date formatting, aggregate-derived values, text-mark hit targets, and area point targets; document the configuration in every generated family Quick API. Raise the default minified bundle budget from 80 KiB to 92 KiB for the native interaction controller while retaining the complete-bundle budget.
- Tighten the Heatmap compiler's default cell spacing to a one-pixel gap with subtle corners, preserving identifiable boundaries while presenting categorical cells as one continuous matrix.
- Expand every consolidated family manual into a visible type gallery with stable per-preset anchors, selection guidance, required fields, and minimal runnable Quick API examples for all 139 family presets; give both declarative adapters the same treatment.
- Merge 114 legacy name-by-name manuals into 37 representative family guides, with functional-difference tables, compiled preset galleries, one compatibility index, and a separate adapter reference.
- Consolidate visually or semantically overlapping names into 37 distinct chart families while retaining all 141 historical names as compatible presets.
- Add explicit family, variant, and mode metadata; resolve all 117 public series identifiers to both a canonical `familyId` and a preserved `variantId`.
- Fold orientation, smoothing, depth, glyph, radius, map layout, financial-body, and 45 indicator differences into discoverable modes instead of separate family cards.
- Regenerate canonical guides, compiled Scene snapshots, and galleries so only distinct families are presented while compatibility APIs continue to compile unchanged.
- Expand the opt-in complete catalog to 141 user-facing families by adding 96 specialized series and mapping the full 117-identifier compatibility surface onto shared canonical implementations.
- Add 32 renderer-neutral mark compilers for range, smooth, distribution, bullet, contour, depth, financial, indicator, geographic, vector, relationship, radial, and text-layout semantics while preserving the 31-family default entrypoint.
- Add a typed specialized-series catalog, Quick APIs for every new family, schema metadata, domain coverage, ESM/browser bundle assertions, and exhaustive compile tests.
- Preserve the 80 KiB default-bundle budget and set a 176 KiB budget for the opt-in complete bundle containing the full resolver catalog.
- Add 96 dedicated manuals, compiled Scene SVG snapshots, and a responsive exact-commit CDN gallery, with deterministic generation and staleness checks in CI.
- Add an opt-in complete catalog entrypoint with Radar, Tree, Graph, Chord, Funnel, Parallel Coordinates, Boxplot, Effect Scatter, Connection Lines, Heatmap, Pictorial Bar, Theme River, Sunburst, and function-free Declarative Custom charts.
- Compile all 14 specialist families into the existing renderer-neutral Scene and Canvas renderer while reusing the established data, scale, theme, interaction, and accessibility contracts.
- Extend portable mark types, specialist domain resolution, package exports, ESM/browser bundles, and schema catalog metadata without changing `ChartSpec 0.1` serialization.
- Add one detailed manual and compiled Scene SVG per specialist family, plus a responsive 14-family complete-bundle gallery.
- Build, size-check, pin, and byte-verify both default and complete browser snapshots through exact-commit CDN URLs and independent SHA-384 integrity values.
- Redesign the default light/dark visual system with a more cohesive categorical palette, quieter axes and grids, improved title spacing, rounded data strokes, outlined points, airier bars, and clearer table styling.
- Render area and stepped-area fills separately from their top strokes so the trend stays crisp without outlining the zero baseline.
- Add percentage-aware pie labels, donut center summaries, gauge ticks and hubs, annotation pills, map surfaces and marker halos, accented organization nodes, curved Sankey bands, and a deterministic two-dimensional treemap layout.
- Regenerate all 31 compiled Scene snapshots and add visual-quality regression coverage for the shared theme and specialist layouts.
- Expand the public catalog to 31 user-facing chart types and compatibility APIs backed by 27 canonical marks or layers.
- Add portable named mark fields and function-free JSON mark options for OHLC, interval, hierarchy, flow, size, date-range, frame, and adapter channels.
- Add Canvas Scene compilers for bubble, calendar, candlestick, diff, Gantt, gauge, geo, histogram, interval, map, motion, organization, pie/donut, Sankey, stepped area, table, timeline, treemap, trendline, waterfall, word tree, annotation, and a safe Vega mark subset.
- Add horizontal categorical y scales, horizontal bars, specialist domain resolution, and whole-polygon hit testing.
- Add a standalone 31-type exact-commit CDN gallery, one detailed guide and compiled visual snapshot per chart, and exhaustive catalog/compile regression tests.
- Restore a clean repository-wide Prettier gate and exclude the generated CDN bundle from source formatting.
- Add reproducible visual snapshots of the implemented output to every chart-specific guide.
- Add detailed, chart-specific guides for bar, line, area, scatter, and shared-scale combination charts.
- Add dedicated line, area, scatter, and mixed-composition chart examples in a responsive chart-type gallery.
- Add `scatter()` as a chart-oriented quick API alias for the portable `point` mark.
- Add regression coverage for line, area, scatter, and mixed chart scene output.
- Extend exact-commit CDN pinning and SRI updates to the standalone chart-type gallery.
- Add dedicated single-series and grouped bar chart examples.
- Add regression coverage for styled, negative-value, and grouped bar rendering.
- Add a reproducible browser snapshot workflow that pins the downloadable jsDelivr example to an exact bundle commit with SHA-384 Subresource Integrity.

## 0.1.0-alpha.0 - 2026-08-19

- Establish the portable `ChartSpec 0.1` contract and JSON Schema.
- Add Canvas-first scene rendering for line, bar, point, and area marks.
- Add shared-scale layer composition, light/dark themes, and plugin registries.
- Add row and zero-copy columnar data ingestion with bounded large-data sampling.
- Add responsive chart instances, structured pointer events, export, tests, and CDN builds.
